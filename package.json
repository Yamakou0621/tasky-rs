{
  "name": "my-monorepo",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "check": "turbo run check"
  },
  "devDependencies": {
    "turbo": "^2.3.2",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
